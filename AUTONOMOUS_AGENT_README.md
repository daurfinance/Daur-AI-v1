# 🤖 Daur AI - Автономный Агент

## 🎯 Что Это?

**Полностью автономный AI агент** для macOS с компьютерным зрением, адаптивным планированием и самокоррекцией.

### ✨ Ключевые Особенности

1. **👁️ Компьютерное Зрение**
   - Видит и анализирует экран через GPT-4 Vision
   - Определяет активные приложения и UI элементы
   - Проверяет результаты действий визуально
   - Определяет раскладку клавиатуры по индикатору

2. **🔍 Профилирование Системы**
   - Автоматически определяет ОС, версию, архитектуру
   - Сканирует установленные приложения
   - Определяет системные горячие клавиши
   - Адаптируется к конфигурации пользователя

3. **🧠 Адаптивное Планирование**
   - Создает планы на основе текущего состояния экрана
   - Учитывает системный профиль
   - Добавляет проверки результатов
   - Включает contingency планы

4. **🔄 Самокоррекция**
   - Проверяет результат каждого действия
   - Адаптирует план при ошибках
   - Пробует альтернативные подходы
   - Учится на ошибках

## 🚀 Быстрый Старт

### 1. Установка Зависимостей

```bash
cd ~/Daur-AI-v1
source venv/bin/activate
pip install -r requirements-macos.txt
```

### 2. Настройка API Ключа

Создайте файл `.env`:

```bash
echo 'OPENAI_API_KEY=your-key-here' > .env
```

### 3. Настройка Разрешений

**Важно!** Дайте Terminal разрешения Accessibility:

1. Откройте **System Settings** (Системные настройки)
2. Перейдите в **Privacy & Security** → **Accessibility**
3. Нажмите **+** и добавьте **Terminal**
4. Убедитесь что галочка включена

### 4. Запуск

```bash
python3 daur_chat_autonomous.py
```

## 📖 Использование

### Примеры Команд

```
Вы: Открой Safari
🤖: ✅ Выполнено! Safari открыт.

Вы: Создай папку 'AI Projects' на рабочем столе
🤖: ✅ Выполнено! Папка создана.

Вы: Открой Калькулятор и вычисли 25*4
🤖: ✅ Выполнено! Результат: 100

Вы: Сделай скриншот
🤖: ✅ Выполнено! Скриншот сохранен.
```

### Что Агент Умеет

- ✅ Открывать приложения
- ✅ Управлять окнами
- ✅ Вводить текст
- ✅ Кликать по элементам
- ✅ Делать скриншоты
- ✅ Искать в интернете
- ✅ Создавать файлы и папки
- ✅ Автоматизировать рутинные задачи

## 🏗️ Архитектура

### Компоненты

```
┌─────────────────────────────────────────┐
│         AutonomousAgent                 │
│  (Главный оркестратор)                  │
└──────────────┬──────────────────────────┘
               │
       ┌───────┴───────┐
       │               │
       ▼               ▼
┌─────────────┐ ┌─────────────┐
│ SystemProfiler│ │VisionAnalyzer│
│ (Профиль)    │ │ (Зрение)    │
└─────────────┘ └─────────────┘
       │               │
       └───────┬───────┘
               ▼
       ┌─────────────┐
       │AdaptivePlanner│
       │ (Планирование)│
       └──────┬────────┘
              │
              ▼
       ┌─────────────┐
       │InputController│
       │ (Управление) │
       └─────────────┘
```

### Цикл Выполнения

```
1. Пользователь → Команда
         ↓
2. Vision → Анализ экрана (до)
         ↓
3. Planner → Создание плана
         ↓
4. Executor → Выполнение действия
         ↓
5. Vision → Анализ экрана (после)
         ↓
6. Verifier → Проверка результата
         ↓
7. [Успех] → Следующее действие
   [Ошибка] → Адаптация плана → Повтор
```

## 🔧 Модули

### 1. SystemProfiler

**Файл:** `src/system/system_profiler.py`

**Функции:**
- Определяет ОС и версию
- Получает разрешение экрана
- Сканирует установленные приложения
- Определяет системные шорткаты
- Определяет раскладку клавиатуры

**Пример:**
```python
profiler = SystemProfiler()
profile = profiler.get_profile()

print(profile['os']['system'])  # Darwin
print(profile['applications'])  # ['Safari', 'Finder', ...]
print(profiler.get_spotlight_shortcut())  # command+space
```

### 2. VisionAnalyzer

**Файл:** `src/vision/vision_analyzer.py`

**Функции:**
- Анализирует скриншоты через GPT-4 Vision
- Определяет видимые приложения
- Находит UI элементы
- Проверяет результаты действий
- Определяет раскладку клавиатуры

**Пример:**
```python
vision = VisionAnalyzer()
analysis = await vision.analyze_screen(
    "screenshot.png",
    context="Looking for Safari"
)

print(analysis['active_app'])  # Safari
print(analysis['spotlight_open'])  # True/False
```

### 3. AdaptivePlanner

**Файл:** `src/ai/adaptive_planner.py`

**Функции:**
- Создает планы на основе контекста
- Учитывает системный профиль
- Адаптирует планы при ошибках
- Добавляет проверки результатов

**Пример:**
```python
planner = AdaptivePlanner()
plan = await planner.create_plan(
    "Open Safari",
    system_profile,
    screen_analysis
)

print(plan.goal)  # Open Safari browser
print(len(plan.actions))  # 5
```

### 4. AutonomousAgent

**Файл:** `src/ai/autonomous_agent.py`

**Функции:**
- Оркестрирует все компоненты
- Выполняет vision loop
- Проверяет результаты
- Самокорректируется

**Пример:**
```python
agent = AutonomousAgent()
await agent.initialize()

result = await agent.process_command("Open Safari")
print(result['success'])  # True
```

## 🎓 Примеры Использования

### Пример 1: Открыть Приложение

```python
agent = AutonomousAgent()
await agent.initialize()

result = await agent.process_command("Открой Safari")
```

**Что происходит:**
1. Vision анализирует текущий экран
2. Planner создает план:
   - Нажать Cmd+Space (Spotlight)
   - Переключить раскладку на английскую
   - Ввести "Safari"
   - Нажать Enter
3. Executor выполняет каждое действие
4. Vision проверяет что Safari открылся
5. Если нет - адаптирует план и пробует снова

### Пример 2: Поиск в Интернете

```python
result = await agent.process_command(
    "Открой Safari и найди информацию об AI"
)
```

**Что происходит:**
1. Открывает Safari (как в примере 1)
2. Vision проверяет что Safari открыт
3. Кликает в адресную строку
4. Вводит поисковый запрос
5. Нажимает Enter
6. Vision проверяет что результаты загрузились

### Пример 3: Создать Папку

```python
result = await agent.process_command(
    "Создай папку 'AI Projects' на рабочем столе"
)
```

**Что происходит:**
1. Открывает Finder
2. Переходит на рабочий стол
3. Создает новую папку (Cmd+Shift+N)
4. Вводит название
5. Vision проверяет что папка создана

## 🐛 Отладка

### Логи

Агент пишет детальные логи:

```python
import logging
logging.basicConfig(level=logging.INFO)
```

### Скриншоты

Все скриншоты сохраняются в:
```
~/.daur_ai/screenshots/
```

### Проверка Компонентов

```bash
# Тест SystemProfiler
python3 src/system/system_profiler.py

# Тест AdaptivePlanner
python3 src/ai/adaptive_planner.py

# Тест AutonomousAgent
python3 src/ai/autonomous_agent.py
```

## ⚙️ Конфигурация

### Изменить Модель

В `src/ai/autonomous_agent.py`:

```python
self.ai = OpenAIClient(api_key=self.api_key, model="gpt-4o")
```

### Изменить Timeout

В `src/ai/autonomous_agent.py`:

```python
await asyncio.sleep(1)  # Изменить на нужное значение
```

### Отключить Vision

Для тестирования без vision:

```python
# Закомментировать в autonomous_agent.py
# screen_state = await self.vision.analyze_screen(...)
screen_state = {"active_app": "unknown", "spotlight_open": False}
```

## 🔒 Безопасность

### Разрешения

Агент требует:
- ✅ Accessibility (для управления)
- ✅ Screen Recording (для скриншотов)

### API Ключ

- Храните ключ в `.env` файле
- Не коммитьте `.env` в git
- Используйте отдельный ключ для разработки

### Sandbox

Агент работает в пределах:
- Пользовательских разрешений macOS
- Доступных приложений
- Файловой системы пользователя

## 📊 Производительность

### Скорость

- Профилирование системы: ~2-3 секунды
- Анализ скриншота: ~1-2 секунды
- Создание плана: ~2-3 секунды
- Выполнение действия: ~1-5 секунд

### Оптимизация

- Кэширование системного профиля
- Параллельный анализ скриншотов
- Пропуск проверок для простых действий

## 🤝 Вклад

Агент открыт для улучшений:

1. Добавить новые типы действий
2. Улучшить vision анализ
3. Оптимизировать планирование
4. Добавить обучение на ошибках

## 📝 Лицензия

MIT License

## 🙏 Благодарности

- OpenAI за GPT-4 Vision
- PyAutoGUI за управление
- Сообщество за фидбек

---

**Создано с ❤️ командой Daur AI**

