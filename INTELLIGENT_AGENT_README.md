# 🤖 Daur AI - Intelligent Agent

## Умный AI Агент с Пониманием Естественного Языка

Daur AI теперь включает **интеллектуального агента**, который понимает команды на естественном языке, планирует действия и автоматически управляет вашим MacBook!

---

## ✨ Возможности

### 🧠 Понимание Естественного Языка

Агент понимает команды на русском и английском:

```
"Открой Safari и найди информацию об AI"
"Создай папку 'Мои Проекты' в Finder"
"Открой Калькулятор и вычисли 25*4"
"Сделай скриншот и сохрани как screenshot.png"
```

### 📋 Планирование и Рассуждение

Агент **сам создает план** выполнения:

1. **Анализирует** команду пользователя
2. **Понимает** намерение и цель
3. **Планирует** последовательность действий
4. **Рассуждает** о каждом шаге
5. **Выполняет** план автоматически

### ⚡ Автономное Выполнение

Агент **сам** управляет компьютером:

- Открывает приложения
- Вводит текст
- Нажимает клавиши
- Управляет мышью
- Делает скриншоты
- Выполняет многошаговые задачи

### 💬 Интерактивный Чат

Общайтесь с агентом как с помощником:

```
Вы: Что ты умеешь?
🤖: Я могу управлять вашим MacBook, открывать приложения...

Вы: Открой Safari
🤖: ✓ Выполнено! Открыл Safari.

Вы: Как создать папку?
🤖: Я могу создать папку за вас. Просто скажите...
```

---

## 🚀 Быстрый Старт

### 1. Обновите Код

```bash
cd ~/Daur-AI-v1
git pull
```

### 2. Убедитесь что API Ключ Установлен

```bash
# Проверьте .env файл
cat .env | grep OPENAI_API_KEY

# Или установите:
export OPENAI_API_KEY='your-key-here'
```

### 3. Запустите Интерактивный Чат

```bash
cd ~/Daur-AI-v1
source venv/bin/activate
python3 daur_chat.py
```

### 4. Попробуйте Команды!

```
Вы: Открой Калькулятор
Вы: Создай папку 'Test' в Finder
Вы: Сделай скриншот
Вы: Открой Safari и найди 'AI automation'
```

---

## 📖 Примеры Использования

### Пример 1: Простая Команда

```
Вы: Открой Safari

🤔 Думаю...

🤖 Daur AI: ✓ Выполнено!

Цель: Открыть Safari browser
Действий выполнено: 4/4

1. Открыть Spotlight (Command+Space)
2. Ввести "Safari"
3. Нажать Enter
4. Подождать 2 секунды
```

### Пример 2: Сложная Автоматизация

```
Вы: Открой Safari, перейди на google.com и найди 'AI automation'

🤔 Думаю...

🤖 Daur AI: ✓ Выполнено!

Цель: Открыть Safari, перейти на Google и выполнить поиск
Действий выполнено: 8/8

План:
1. Открыть Safari через Spotlight
2. Подождать загрузки
3. Открыть новую вкладку (Cmd+T)
4. Ввести "google.com"
5. Нажать Enter
6. Подождать загрузки
7. Ввести поисковый запрос
8. Нажать Enter для поиска
```

### Пример 3: Создание Файлов

```
Вы: Создай папку 'AI Projects' в Finder

🤔 Думаю...

🤖 Daur AI: ✓ Выполнено!

Цель: Создать новую папку в Finder
Действий выполнено: 5/5

1. Открыть Finder
2. Нажать Cmd+Shift+N (новая папка)
3. Ввести название "AI Projects"
4. Нажать Enter
5. Папка создана!
```

### Пример 4: Скриншоты

```
Вы: Сделай скриншот и сохрани как my_screen.png

🤔 Думаю...

🤖 Daur AI: ✓ Выполнено!

Цель: Захватить экран и сохранить
Действий выполнено: 1/1

📸 Скриншот сохранен: my_screen.png
```

---

## 🎯 Что Агент Умеет

### Управление Приложениями

```
"Открой Safari"
"Запусти Калькулятор"
"Открой Finder"
"Запусти Notes"
"Открой Terminal"
```

### Автоматизация Браузера

```
"Открой Safari и перейди на google.com"
"Найди в Google информацию об AI"
"Открой новую вкладку в браузере"
"Закрой текущую вкладку"
```

### Работа с Файлами

```
"Создай папку 'Проекты'"
"Создай папку 'Documents/AI' в Finder"
"Открой Finder и создай новую папку"
```

### Ввод Текста

```
"Открой Notes и напиши 'Список дел'"
"Введи текст 'Hello World'"
"Напиши в калькуляторе 2+2"
```

### Скриншоты

```
"Сделай скриншот"
"Сохрани скриншот как screenshot.png"
"Захвати экран"
```

### Комбинированные Задачи

```
"Открой Safari, перейди на github.com и найди 'AI automation'"
"Создай папку 'Projects', открой Notes и напиши 'Новый проект'"
"Открой Калькулятор, вычисли 25*4 и сделай скриншот"
```

---

## 🧪 Демонстрация

Запустите демонстрацию чтобы увидеть все возможности:

```bash
cd ~/Daur-AI-v1
source venv/bin/activate
python3 examples/intelligent_agent_demo.py
```

**Демонстрация покажет:**

1. **Simple Commands** - Простые команды
2. **Complex Automation** - Сложная автоматизация
3. **Planning & Reasoning** - Планирование и рассуждение
4. **Interactive Chat** - Интерактивный чат

---

## 💻 Программное Использование

### Базовое Использование

```python
import asyncio
from src.ai.intelligent_agent import IntelligentAgent

async def main():
    # Создать агента
    agent = IntelligentAgent()
    
    # Выполнить команду
    result = await agent.process_command("Открой Safari")
    
    if result['success']:
        print(f"✓ Успех!")
        print(f"План: {result['plan']['goal']}")
        print(f"Действий: {len(result['plan']['actions'])}")
    else:
        print(f"✗ Ошибка: {result['error']}")

asyncio.run(main())
```

### Интерактивный Чат

```python
import asyncio
from src.ai.intelligent_agent import IntelligentAgent

async def chat_example():
    agent = IntelligentAgent()
    
    # Задать вопрос
    response = await agent.chat("Что ты умеешь?")
    print(response)
    
    # Выполнить команду
    response = await agent.chat("Открой Калькулятор")
    print(response)

asyncio.run(chat_example())
```

### Детальное Планирование

```python
import asyncio
from src.ai.intelligent_agent import IntelligentAgent

async def detailed_planning():
    agent = IntelligentAgent()
    
    command = "Создай папку 'Test' в Finder"
    
    # Понять команду
    understanding = await agent._understand_command(command)
    print(f"Намерение: {understanding['intent']}")
    
    # Создать план
    plan = await agent._create_plan(command, understanding)
    print(f"План: {plan.goal}")
    print(f"Действий: {len(plan.actions)}")
    
    for i, action in enumerate(plan.actions, 1):
        print(f"{i}. {action.description}")
        print(f"   Рассуждение: {action.reasoning}")
    
    # Выполнить план
    result = await agent._execute_plan(plan)
    print(f"Выполнено: {result['successful_steps']}/{result['total_steps']}")

asyncio.run(detailed_planning())
```

---

## 🔧 Как Это Работает

### Архитектура

```
Пользователь
    ↓
[Естественный Язык]
    ↓
IntelligentAgent
    ↓
┌─────────────────────┐
│ 1. Understanding    │ ← OpenAI GPT-4
│    Понимание        │
└─────────────────────┘
    ↓
┌─────────────────────┐
│ 2. Planning         │ ← OpenAI GPT-4
│    Планирование     │
└─────────────────────┘
    ↓
┌─────────────────────┐
│ 3. Execution        │ ← InputController
│    Выполнение       │   ScreenCapture
└─────────────────────┘
    ↓
[Результат]
```

### Фаза 1: Понимание

```python
# Агент анализирует команду
understanding = {
    "intent": "open_app",
    "target": "Safari",
    "complexity": "simple",
    "requires_planning": true
}
```

### Фаза 2: Планирование

```python
# Агент создает детальный план
plan = {
    "goal": "Открыть Safari browser",
    "reasoning": "Использовать Spotlight для быстрого запуска",
    "actions": [
        {
            "type": "hotkey",
            "description": "Открыть Spotlight",
            "parameters": {"keys": ["command", "space"]},
            "reasoning": "Spotlight - быстрый способ запуска"
        },
        {
            "type": "type_text",
            "description": "Ввести 'Safari'",
            "parameters": {"text": "Safari"},
            "reasoning": "Найти приложение Safari"
        },
        # ...
    ]
}
```

### Фаза 3: Выполнение

```python
# Агент выполняет каждое действие
for action in plan.actions:
    await execute_action(action)
    # Контролирует мышь, клавиатуру, делает скриншоты
```

---

## 🎓 Продвинутые Примеры

### Пример 1: Веб-скрапинг

```python
async def web_scraping():
    agent = IntelligentAgent()
    
    commands = [
        "Открой Safari и перейди на news.ycombinator.com",
        "Сделай скриншот страницы как hn.png",
        "Прокрути страницу вниз",
        "Сделай еще один скриншот как hn2.png"
    ]
    
    for cmd in commands:
        await agent.process_command(cmd)
        await asyncio.sleep(2)
```

### Пример 2: Автоматизация Работы

```python
async def daily_routine():
    agent = IntelligentAgent()
    
    routine = [
        "Открой Safari и перейди на gmail.com",
        "Открой новую вкладку и перейди на calendar.google.com",
        "Открой Finder и создай папку с сегодняшней датой",
        "Открой Notes и напиши 'План на день'"
    ]
    
    for task in routine:
        result = await agent.process_command(task)
        print(f"✓ {task}")
```

### Пример 3: Тестирование UI

```python
async def ui_testing():
    agent = IntelligentAgent()
    
    test_steps = [
        "Открой Safari",
        "Перейди на localhost:3000",
        "Сделай скриншот как before.png",
        "Кликни на кнопку Login",
        "Введи username: test@example.com",
        "Введи password: test123",
        "Нажми Enter",
        "Сделай скриншот как after.png"
    ]
    
    for step in test_steps:
        await agent.process_command(step)
```

---

## 🛠️ Настройка

### Изменить Модель AI

```python
# В src/ai/intelligent_agent.py
self.ai = OpenAIClient(
    api_key=self.api_key,
    model="gpt-4"  # Или "gpt-3.5-turbo" для скорости
)
```

### Добавить Новые Действия

```python
# В src/ai/intelligent_agent.py, метод _execute_action()

elif action_type == "custom_action":
    # Ваша логика
    param = params.get('param', 'default')
    # Выполнить действие
    return f"Custom action executed"
```

### Настроить Промпты

Промпты находятся в методах:
- `_understand_command()` - Понимание команды
- `_create_plan()` - Создание плана

Измените их для улучшения понимания или добавления новых возможностей.

---

## 📊 Производительность

- **Понимание команды**: ~1-2 секунды
- **Создание плана**: ~2-3 секунды
- **Выполнение**: Зависит от количества действий
- **Простая команда**: ~5-10 секунд
- **Сложная автоматизация**: ~15-30 секунд

---

## 🔒 Безопасность

Агент работает с теми же разрешениями что и Terminal:

- ✅ Требует Accessibility разрешения
- ✅ Требует Screen Recording разрешения
- ✅ Не может делать ничего что не может Terminal
- ✅ Все действия логируются

---

## 🐛 Troubleshooting

### Агент не запускается

```bash
# Проверьте API ключ
echo $OPENAI_API_KEY

# Или в .env
cat .env | grep OPENAI_API_KEY
```

### Команды не выполняются

```bash
# Проверьте разрешения macOS
# System Settings → Privacy & Security → Accessibility
# Добавьте Terminal
```

### Ошибки импорта

```bash
# Убедитесь что виртуальное окружение активно
source venv/bin/activate

# Переустановите зависимости
pip install -r requirements-macos.txt
```

---

## 🎉 Заключение

**Daur AI Intelligent Agent** - это мощный инструмент для автоматизации MacBook через естественный язык!

**Возможности:**
- ✅ Понимание естественного языка
- ✅ AI-планирование и рассуждение
- ✅ Автономное выполнение
- ✅ Интерактивный чат
- ✅ Многошаговая автоматизация

**Попробуйте сейчас:**

```bash
cd ~/Daur-AI-v1
source venv/bin/activate
python3 daur_chat.py
```

---

## 📚 Дополнительные Ресурсы

- **API Documentation**: `docs/api/`
- **User Guides**: `docs/guides/`
- **Examples**: `examples/`
- **GitHub**: https://github.com/daurfinance/Daur-AI-v1

---

**Создано с ❤️ командой Daur AI**

