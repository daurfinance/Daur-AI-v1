# 🤖 Local LLM Architecture for MVP

## Goal
**100% локальное решение без API** - все работает на компьютере клиента

---

## 🆓 Локальная Языковая Модель

### Выбор: Ollama + Llama 3.2

**Почему Ollama?**
- ✅ Бесплатно и open source
- ✅ Простая установка (одна команда)
- ✅ Поддержка macOS, Windows, Linux
- ✅ Автоматическое управление моделями
- ✅ REST API для интеграции
- ✅ Поддержка vision моделей

**Почему Llama 3.2?**
- ✅ Лучшая open source модель (от Meta)
- ✅ Хорошее качество reasoning
- ✅ Поддержка vision (Llama 3.2-Vision)
- ✅ Разные размеры (1B, 3B, 11B, 90B)
- ✅ Быстрая работа

---

## 📦 Установка

### Шаг 1: Установить Ollama

**macOS:**
```bash
brew install ollama
```

**Linux:**
```bash
curl -fsSL https://ollama.com/install.sh | sh
```

**Windows:**
```powershell
# Скачать с https://ollama.com/download
```

### Шаг 2: Скачать Модели

**Основная модель (текст):**
```bash
# Llama 3.2 3B (быстрая, для обычных компьютеров)
ollama pull llama3.2:3b

# Или Llama 3.2 11B (лучше качество, нужен мощный компьютер)
ollama pull llama3.2:11b
```

**Vision модель (анализ скриншотов):**
```bash
# Llama 3.2-Vision 11B
ollama pull llama3.2-vision:11b

# Или LLaVA (альтернатива)
ollama pull llava
```

**Coding модель (для генерации кода):**
```bash
# CodeLlama (специализирована на коде)
ollama pull codellama:7b
```

---

## 🏗️ Архитектура

### Компоненты

```
Local AI Stack:
┌─────────────────────────────────────┐
│         Daur AI MVP Agent           │
│  (Python, координирует все)         │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│          Ollama Server              │
│  (Управляет моделями)                │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│         Local Models                │
│  • Llama 3.2 (reasoning)            │
│  • Llama 3.2-Vision (screen)        │
│  • CodeLlama (coding)               │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│      Client Hardware (GPU/CPU)      │
└─────────────────────────────────────┘
```

---

## 💻 Использование в Коде

### Базовое Использование

```python
import requests
import json

class LocalLLM:
    def __init__(self, model="llama3.2:3b"):
        self.model = model
        self.api_url = "http://localhost:11434/api/generate"
    
    def chat(self, prompt: str) -> str:
        """Отправить промпт в локальную модель"""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "stream": False
        }
        
        response = requests.post(self.api_url, json=payload)
        result = response.json()
        return result['response']
    
    def chat_with_vision(self, prompt: str, image_path: str) -> str:
        """Анализ скриншота локально"""
        # Читаем изображение
        import base64
        with open(image_path, 'rb') as f:
            image_data = base64.b64encode(f.read()).decode()
        
        payload = {
            "model": "llama3.2-vision:11b",
            "prompt": prompt,
            "images": [image_data],
            "stream": False
        }
        
        response = requests.post(self.api_url, json=payload)
        result = response.json()
        return result['response']

# Использование
llm = LocalLLM()

# Текстовый запрос
response = llm.chat("What should I do to open Safari?")
print(response)

# Анализ скриншота
analysis = llm.chat_with_vision(
    prompt="What applications are visible on this screen?",
    image_path="/path/to/screenshot.png"
)
print(analysis)
```

---

## 🎯 Специализированные Модели

### 1. Reasoning Model (Llama 3.2)

**Для:** Планирование, принятие решений

```python
reasoning_llm = LocalLLM(model="llama3.2:3b")

plan = reasoning_llm.chat("""
Task: Open Safari and search for "AI automation"

Create a step-by-step plan:
""")

print(plan)
# Output:
# 1. Press Cmd+Space to open Spotlight
# 2. Type "Safari"
# 3. Press Enter
# 4. Wait for Safari to open
# 5. Press Cmd+L to focus address bar
# 6. Type "AI automation"
# 7. Press Enter
```

---

### 2. Vision Model (Llama 3.2-Vision)

**Для:** Анализ скриншотов, понимание UI

```python
vision_llm = LocalLLM(model="llama3.2-vision:11b")

# Анализ экрана
screen_analysis = vision_llm.chat_with_vision(
    prompt="What is the current state of the screen? List all visible UI elements.",
    image_path="screenshot.png"
)

# Поиск элементов
button_location = vision_llm.chat_with_vision(
    prompt="Where is the 'New Tab' button located? Provide coordinates.",
    image_path="screenshot.png"
)
```

---

### 3. Coding Model (CodeLlama)

**Для:** Генерация кода, рефакторинг

```python
code_llm = LocalLLM(model="codellama:7b")

code = code_llm.chat("""
Write a Python function that:
1. Takes a screenshot
2. Saves it to a file
3. Returns the file path

Use pyautogui library.
""")

print(code)
# Output: полный рабочий код функции
```

---

## ⚡ Производительность

### Требования к Железу

| Модель | RAM | GPU | Скорость (tokens/sec) |
|--------|-----|-----|----------------------|
| Llama 3.2 1B | 2GB | Опционально | 50-100 |
| Llama 3.2 3B | 4GB | Опционально | 30-60 |
| Llama 3.2 11B | 8GB | Рекомендуется | 10-30 |
| Llama 3.2-Vision 11B | 12GB | Рекомендуется | 5-15 |
| CodeLlama 7B | 6GB | Опционально | 20-40 |

### Оптимизация

**Для слабых компьютеров:**
```bash
# Используйте квантизованные модели (меньше памяти, быстрее)
ollama pull llama3.2:3b-q4_0  # 4-bit quantization
```

**Для мощных компьютеров:**
```bash
# Используйте полные модели (лучше качество)
ollama pull llama3.2:11b
```

---

## 🔄 Fallback Стратегия

### Гибридный Подход

```python
class HybridLLM:
    def __init__(self):
        self.local_llm = LocalLLM(model="llama3.2:3b")
        self.use_cloud = False  # По умолчанию локально
    
    async def chat(self, prompt: str) -> str:
        try:
            # Попробовать локально
            response = self.local_llm.chat(prompt)
            return response
        except Exception as e:
            # Если локальная модель не работает
            if self.use_cloud:
                # Fallback к cloud API (опционально)
                response = await openai.chat(prompt)
                return response
            else:
                raise e
```

---

## 📊 Сравнение: Local vs Cloud

| Параметр | Local (Ollama) | Cloud (OpenAI) |
|----------|----------------|----------------|
| **Стоимость** | $0 (бесплатно) | $0.01-0.05 за запрос |
| **Скорость** | 1-5 сек | 2-6 сек |
| **Качество** | Хорошее (80-90%) | Отличное (95-99%) |
| **Приватность** | 100% приватно | Данные уходят в cloud |
| **Оффлайн** | ✅ Работает | ❌ Нужен интернет |
| **Требования** | 4-12GB RAM | Только интернет |

---

## 🎯 Рекомендация для MVP

### Оптимальная Конфигурация

**Для обычных компьютеров (8GB RAM):**
```bash
ollama pull llama3.2:3b        # Основная модель
ollama pull llava              # Vision (легче чем Llama 3.2-Vision)
```

**Для мощных компьютеров (16GB+ RAM):**
```bash
ollama pull llama3.2:11b           # Основная модель
ollama pull llama3.2-vision:11b    # Vision
ollama pull codellama:7b           # Coding
```

---

## 🚀 Преимущества Локального Решения

1. **Бесплатно** - нет API costs
2. **Приватно** - все данные остаются на компьютере
3. **Быстро** - нет сетевых задержек
4. **Оффлайн** - работает без интернета
5. **Масштабируемо** - можно запускать сколько угодно запросов
6. **Кастомизируемо** - можно fine-tune модели

---

## 📝 Установка для Клиента

### Автоматический Скрипт

```bash
#!/bin/bash
# setup_local_ai.sh

echo "🤖 Установка локального AI для Daur MVP..."

# 1. Установить Ollama
echo "📦 Устанавливаю Ollama..."
brew install ollama

# 2. Запустить Ollama сервер
echo "🚀 Запускаю Ollama сервер..."
ollama serve &

# 3. Скачать модели
echo "⬇️ Скачиваю модели (это займет 5-10 минут)..."
ollama pull llama3.2:3b
ollama pull llava

echo "✅ Готово! Локальный AI установлен!"
```

---

## 🎉 Итог

**MVP будет использовать:**
- ✅ **Ollama** - управление моделями
- ✅ **Llama 3.2** - reasoning и планирование
- ✅ **LLaVA** - анализ скриншотов
- ✅ **CodeLlama** (опционально) - генерация кода

**Все работает локально, бесплатно, приватно!** 🚀

