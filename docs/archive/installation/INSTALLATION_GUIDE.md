# Daur-AI v2.0: Полное Руководство по Установке и Использованию

**Версия:** 2.0.0  
**Дата:** 25 октября 2025  
**Автор:** Manus AI  
**Язык:** Русский / English

---

## 📖 Оглавление

1. [Системные требования](#системные-требования)
2. [Установка на macOS](#установка-на-macos)
3. [Установка на Windows](#установка-на-windows)
4. [Установка на Linux](#установка-на-linux)
5. [Первоначальная настройка](#первоначальная-настройка)
6. [Использование](#использование)
7. [API Документация](#api-документация)
8. [Решение проблем](#решение-проблем)
9. [Контакты и поддержка](#контакты-и-поддержка)

---

## 🖥️ Системные требования

### Минимальные требования

| Параметр | Требование |
|----------|-----------|
| **ОС** | macOS 10.13+, Windows 10+, Ubuntu 18.04+ |
| **Процессор** | Intel/AMD x86-64 или Apple Silicon |
| **Оперативная память** | 4 GB (рекомендуется 8 GB) |
| **Свободное место** | 2 GB |
| **Python** | 3.11+ |
| **Интернет** | Требуется для первоначальной установки |

### Рекомендуемые требования

| Параметр | Рекомендация |
|----------|------------|
| **ОС** | macOS 12+, Windows 11, Ubuntu 22.04+ |
| **Процессор** | Apple Silicon M1/M2 или Intel i7/i9 |
| **Оперативная память** | 16 GB |
| **Свободное место** | 5 GB |
| **GPU** | NVIDIA RTX 3060+ (опционально для ускорения) |

---

## 🍎 Установка на macOS

### Способ 1: Использование DMG установщика (рекомендуется)

**Шаг 1:** Скачайте DMG файл
```bash
# Скачайте Daur-AI-2.0.0.dmg с официального сайта
# или используйте wget:
wget https://releases.daur-ai.com/Daur-AI-2.0.0.dmg
```

**Шаг 2:** Откройте DMG файл
```bash
# Дважды щелкните на Daur-AI-2.0.0.dmg в Finder
# или используйте командную строку:
open Daur-AI-2.0.0.dmg
```

**Шаг 3:** Перетащите приложение в папку Applications
```bash
# Перетащите Daur-AI.app в папку Applications
# Или используйте командную строку:
cp -r /Volumes/Daur-AI/Daur-AI.app /Applications/
```

**Шаг 4:** Запустите приложение
```bash
# Откройте Daur-AI из Launchpad или используйте:
open /Applications/Daur-AI.app
```

### Способ 2: Использование PKG установщика

**Шаг 1:** Скачайте PKG файл
```bash
wget https://releases.daur-ai.com/Daur-AI-2.0.0.pkg
```

**Шаг 2:** Установите приложение
```bash
# Дважды щелкните на Daur-AI-2.0.0.pkg
# Или используйте командную строку:
sudo installer -pkg Daur-AI-2.0.0.pkg -target /
```

**Шаг 3:** Запустите приложение
```bash
open /Applications/Daur-AI.app
```

### Способ 3: Установка из исходного кода

**Шаг 1:** Клонируйте репозиторий
```bash
git clone https://github.com/daurfinance/Daur-AI-v1.git
cd Daur-AI-v1
```

**Шаг 2:** Создайте виртуальное окружение
```bash
python3.11 -m venv venv
source venv/bin/activate
```

**Шаг 3:** Установите зависимости
```bash
pip install -r requirements.txt
```

**Шаг 4:** Запустите приложение
```bash
python src/main.py
```

### Решение проблем на macOS

**Проблема:** "Daur-AI не может быть открыто, потому что разработчик не проверен"

**Решение:**
```bash
# Разрешите приложению запускаться
sudo xattr -rd com.apple.quarantine /Applications/Daur-AI.app
```

**Проблема:** "Permission denied" при запуске

**Решение:**
```bash
# Дайте права на выполнение
chmod +x /Applications/Daur-AI.app/Contents/MacOS/Daur-AI
```

---

## 🪟 Установка на Windows

### Способ 1: Использование EXE установщика (рекомендуется)

**Шаг 1:** Скачайте EXE файл
```powershell
# Скачайте Daur-AI-2.0.0.exe с официального сайта
# или используйте PowerShell:
Invoke-WebRequest -Uri "https://releases.daur-ai.com/Daur-AI-2.0.0.exe" -OutFile "Daur-AI-2.0.0.exe"
```

**Шаг 2:** Запустите установщик
```powershell
# Дважды щелкните на Daur-AI-2.0.0.exe
# Или используйте PowerShell:
.\Daur-AI-2.0.0.exe
```

**Шаг 3:** Следуйте инструкциям установщика
- Выберите язык установки
- Примите лицензионное соглашение
- Выберите папку установки (по умолчанию: C:\Program Files\Daur-AI)
- Выберите компоненты для установки
- Завершите установку

**Шаг 4:** Запустите приложение
```powershell
# Найдите Daur-AI в меню Пуск
# Или используйте PowerShell:
& "C:\Program Files\Daur-AI\Daur-AI.exe"
```

### Способ 2: Использование MSI установщика

**Шаг 1:** Скачайте MSI файл
```powershell
Invoke-WebRequest -Uri "https://releases.daur-ai.com/Daur-AI-2.0.0.msi" -OutFile "Daur-AI-2.0.0.msi"
```

**Шаг 2:** Установите приложение
```powershell
# Дважды щелкните на Daur-AI-2.0.0.msi
# Или используйте PowerShell:
msiexec /i Daur-AI-2.0.0.msi /quiet
```

### Способ 3: Установка из исходного кода

**Шаг 1:** Установите Python 3.11
```powershell
# Скачайте Python с https://www.python.org/downloads/
# Или используйте Chocolatey:
choco install python311
```

**Шаг 2:** Клонируйте репозиторий
```powershell
git clone https://github.com/daurfinance/Daur-AI-v1.git
cd Daur-AI-v1
```

**Шаг 3:** Создайте виртуальное окружение
```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
```

**Шаг 4:** Установите зависимости
```powershell
pip install -r requirements.txt
```

**Шаг 5:** Запустите приложение
```powershell
python src/main.py
```

### Решение проблем на Windows

**Проблема:** "Windows Defender заблокировал приложение"

**Решение:**
1. Нажмите "Дополнительно"
2. Нажмите "Все равно запустить"
3. Подтвердите действие

**Проблема:** "Python не найден"

**Решение:**
```powershell
# Добавьте Python в PATH:
$env:Path += ";C:\Python311"
```

---

## 🐧 Установка на Linux

### Ubuntu/Debian

**Шаг 1:** Обновите пакеты
```bash
sudo apt update && sudo apt upgrade -y
```

**Шаг 2:** Установите зависимости
```bash
sudo apt install -y python3.11 python3.11-venv python3.11-dev git curl
```

**Шаг 3:** Клонируйте репозиторий
```bash
git clone https://github.com/daurfinance/Daur-AI-v1.git
cd Daur-AI-v1
```

**Шаг 4:** Создайте виртуальное окружение
```bash
python3.11 -m venv venv
source venv/bin/activate
```

**Шаг 5:** Установите зависимости
```bash
pip install -r requirements.txt
```

**Шаг 6:** Запустите приложение
```bash
python src/main.py
```

### CentOS/RHEL

**Шаг 1:** Установите зависимости
```bash
sudo yum install -y python3.11 python3.11-devel git curl
```

**Шаг 2:** Следуйте шагам 3-6 из Ubuntu/Debian

### Arch Linux

**Шаг 1:** Установите зависимости
```bash
sudo pacman -S python git curl
```

**Шаг 2:** Следуйте шагам 3-6 из Ubuntu/Debian

---

## ⚙️ Первоначальная настройка

### Конфигурация приложения

**Шаг 1:** Откройте файл конфигурации
```bash
# На macOS/Linux:
nano ~/.daur-ai/config.json

# На Windows:
notepad %APPDATA%\Daur-AI\config.json
```

**Шаг 2:** Установите параметры
```json
{
  "api_key": "your-api-key-here",
  "api_url": "https://api.daur-ai.com",
  "language": "ru",
  "theme": "dark",
  "auto_update": true,
  "log_level": "info"
}
```

**Шаг 3:** Сохраните файл

### Интеграция с OpenAI (опционально)

**Шаг 1:** Получите API ключ OpenAI
```bash
# Перейдите на https://platform.openai.com/api-keys
# Создайте новый ключ
```

**Шаг 2:** Установите ключ в конфигурацию
```bash
# На macOS/Linux:
export OPENAI_API_KEY="your-openai-key-here"

# На Windows:
set OPENAI_API_KEY=your-openai-key-here
```

### Интеграция с Docker (опционально)

**Шаг 1:** Установите Docker
```bash
# На macOS:
brew install docker

# На Windows:
# Скачайте Docker Desktop с https://www.docker.com/products/docker-desktop

# На Linux:
sudo apt install docker.io
```

**Шаг 2:** Запустите Docker контейнер
```bash
docker build -t daur-ai .
docker run -it daur-ai
```

---

## 🚀 Использование

### Запуск приложения

**Через GUI:**
```bash
# На macOS:
open /Applications/Daur-AI.app

# На Windows:
"C:\Program Files\Daur-AI\Daur-AI.exe"

# На Linux:
./daur-ai
```

**Через командную строку:**
```bash
python src/main.py --help
```

### Основные команды

```bash
# Запуск с конкретным сценарием
python src/main.py --scenario automation

# Запуск с отладкой
python src/main.py --debug

# Запуск с логированием
python src/main.py --log-level debug

# Запуск API сервера
python src/web/enhanced_api_server.py

# Запуск WebSocket сервера
python src/web/websocket_manager.py
```

### Примеры использования

**Пример 1: Автоматизация браузера**
```python
from src.browser.browser_automation import BrowserAutomation

browser = BrowserAutomation()
browser.open_url("https://example.com")
browser.click_element("button.submit")
browser.type_text("input.search", "Daur-AI")
```

**Пример 2: Управление мышкой и клавиатурой**
```python
from src.input.mouse_controller import MouseController
from src.input.keyboard_controller import KeyboardController

mouse = MouseController()
mouse.move_to(100, 100)
mouse.click()

keyboard = KeyboardController()
keyboard.type_text("Hello, Daur-AI!")
keyboard.hotkey("ctrl", "a")
```

**Пример 3: Анализ скриншотов**
```python
from src.vision.screen_recognition import ScreenRecognition

vision = ScreenRecognition()
screenshot = vision.capture_screen()
objects = vision.detect_objects(screenshot)
text = vision.extract_text(screenshot)
```

---

## 📚 API Документация

### REST API

#### Базовый URL
```
https://api.daur-ai.com/api/v2
```

#### Аутентификация
```bash
# Все запросы должны включать API ключ:
curl -H "Authorization: Bearer YOUR_API_KEY" https://api.daur-ai.com/api/v2/status
```

#### Endpoints

**Управление мышкой:**
```bash
# Переместить мышь
POST /mouse/move
{
  "x": 100,
  "y": 100,
  "duration": 0.5
}

# Нажать кнопку мыши
POST /mouse/click
{
  "x": 100,
  "y": 100,
  "button": "left"
}

# Найти изображение
POST /mouse/find-image
{
  "image_path": "/path/to/image.png",
  "confidence": 0.9
}
```

**Управление клавиатурой:**
```bash
# Напечатать текст
POST /keyboard/type
{
  "text": "Hello, Daur-AI!",
  "interval": 0.05
}

# Горячие клавиши
POST /keyboard/hotkey
{
  "keys": ["ctrl", "a"]
}
```

**Управление оборудованием:**
```bash
# Получить статус оборудования
GET /hardware/status

# Получить информацию о GPU
GET /hardware/gpu

# Получить информацию о батарее
GET /hardware/battery

# Получить информацию о температуре
GET /hardware/temperature
```

**Управление сетью:**
```bash
# Получить статус сети
GET /network/status

# Получить информацию об интерфейсах
GET /network/interfaces
```

**Компьютерное зрение:**
```bash
# Детектировать лица
POST /vision/faces/detect
{
  "image_path": "/path/to/image.png"
}

# Детектировать штрих-коды
POST /vision/barcodes/detect
{
  "image_path": "/path/to/image.png"
}

# Извлечь текст
POST /vision/text/extract
{
  "image_path": "/path/to/image.png"
}
```

**Статус системы:**
```bash
# Получить статус API
GET /status

# Получить здоровье системы
GET /health
```

### WebSocket API

#### Подключение
```javascript
const ws = new WebSocket('wss://api.daur-ai.com/ws');

ws.onopen = function() {
    console.log('Connected');
};

ws.onmessage = function(event) {
    console.log('Message:', event.data);
};
```

#### События

**Движение мыши в real-time:**
```javascript
ws.send(JSON.stringify({
    event: 'mouse_move',
    data: { x: 100, y: 100 }
}));
```

**Печать текста в real-time:**
```javascript
ws.send(JSON.stringify({
    event: 'keyboard_type',
    data: { text: 'Hello' }
}));
```

**Получение статуса оборудования:**
```javascript
ws.send(JSON.stringify({
    event: 'hardware_status',
    data: {}
}));
```

---

## 🔧 Решение проблем

### Общие проблемы

**Проблема:** Приложение не запускается

**Решение:**
```bash
# Проверьте логи
cat ~/.daur-ai/logs/error.log

# Переустановите зависимости
pip install --upgrade -r requirements.txt

# Очистите кэш
rm -rf ~/.daur-ai/cache
```

**Проблема:** Высокое использование памяти

**Решение:**
```bash
# Уменьшите размер кэша в конфигурации
# Отключите ненужные модули
# Перезагрузите приложение
```

**Проблема:** Медленная работа

**Решение:**
```bash
# Включите оптимизацию производительности
python src/main.py --optimize

# Уменьшите разрешение экрана для анализа
# Отключите логирование в production
```

### Проблемы с API

**Проблема:** "401 Unauthorized"

**Решение:**
```bash
# Проверьте API ключ
echo $OPENAI_API_KEY

# Обновите ключ в конфигурации
```

**Проблема:** "Connection timeout"

**Решение:**
```bash
# Проверьте интернет соединение
ping api.daur-ai.com

# Увеличьте timeout в конфигурации
```

---

## 📞 Контакты и поддержка

### Официальные каналы

| Канал | Контакт |
|-------|---------|
| **Telegram** | [@daur.abd](https://t.me/daur.abd) |
| **Email** | support@daur-ai.com |
| **Веб-сайт** | https://daur-ai.com |
| **GitHub** | https://github.com/daurfinance/Daur-AI-v1 |
| **Discord** | https://discord.gg/daur-ai |

### Получение помощи

1. **Проверьте документацию** - Большинство вопросов рассмотрены в этом руководстве
2. **Поищите в GitHub Issues** - Возможно, ваша проблема уже решена
3. **Создайте новый Issue** - Если проблема не решена, создайте новый Issue на GitHub
4. **Свяжитесь с поддержкой** - Напишите в Telegram или на email

### Сообщение об ошибках

При сообщении об ошибке, пожалуйста, включите:
- Версию приложения (`python src/main.py --version`)
- Операционную систему и версию
- Полный текст ошибки
- Шаги для воспроизведения проблемы
- Логи (`~/.daur-ai/logs/`)

---

## 📝 Лицензия

Daur-AI v2.0 распространяется под лицензией MIT. Подробнее см. в файле LICENSE.

---

**Спасибо за использование Daur-AI!** 🚀

Для получения последних обновлений и новостей подпишитесь на наш Telegram канал: [@daur.abd](https://t.me/daur.abd)

---

**Версия:** 2.0.0  
**Последнее обновление:** 25 октября 2025  
**Автор:** Manus AI

