# Daur-AI v2.0 - Полное Руководство Установки на MacBook

## 📋 Содержание
1. [Требования](#требования)
2. [Установка зависимостей](#установка-зависимостей)
3. [Установка Daur-AI](#установка-daur-ai)
4. [Запуск приложения](#запуск-приложения)
5. [Проверка работы](#проверка-работы)
6. [Решение проблем](#решение-проблем)

---

## 📦 Требования

**Минимальные требования:**
- MacBook с процессором Intel или Apple Silicon (M1/M2/M3)
- macOS 10.14 или выше
- 8GB оперативной памяти (рекомендуется 16GB)
- 5GB свободного места на диске
- Интернет соединение

**Рекомендуемые требования:**
- MacBook Pro или MacBook Air
- macOS 12.0 или выше
- 16GB оперативной памяти
- 10GB свободного места на диске
- SSD диск

---

## 🔧 Установка Зависимостей

### Шаг 1: Установить Homebrew (если не установлен)

Homebrew - это менеджер пакетов для macOS.

```bash
# Откройте Terminal (Cmd + Space, введите "Terminal")

# Установите Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Проверьте установку
brew --version
# Должно вывести версию, например: Homebrew 4.0.0
```

**Если вы видите ошибку "Command not found":**
- Закройте Terminal полностью
- Откройте Terminal заново
- Попробуйте ещё раз

---

### Шаг 2: Установить Python 3.11

```bash
# Установите Python 3.11 через Homebrew
brew install python@3.11

# Проверьте версию
python3.11 --version
# Должно вывести: Python 3.11.x

# Создайте символическую ссылку (опционально)
brew link python@3.11
```

---

### Шаг 3: Установить Git

```bash
# Установите Git
brew install git

# Проверьте версию
git --version
# Должно вывести версию Git
```

---

### Шаг 4: Установить Docker Desktop для Mac

Docker позволяет запустить приложение в контейнере.

**Способ 1: Через Homebrew (рекомендуется)**
```bash
# Установите Docker
brew install --cask docker

# Откройте Docker из Applications
# Или используйте Spotlight: Cmd + Space, введите "Docker"
```

**Способ 2: Прямая загрузка**
1. Перейдите на https://www.docker.com/products/docker-desktop
2. Нажмите "Download for Mac"
3. Выберите версию для вашего процессора:
   - **Apple Silicon (M1/M2/M3):** "Apple Silicon"
   - **Intel:** "Intel Chip"
4. Откройте загруженный файл и перетащите Docker в Applications

**Проверка установки Docker:**
```bash
# Откройте новый Terminal и проверьте
docker --version
# Должно вывести: Docker version x.x.x

# Проверьте, что Docker запущен
docker ps
# Должно вывести пустой список контейнеров
```

---

### Шаг 5: Установить Необходимые Инструменты

```bash
# Установите Xcode Command Line Tools (если не установлены)
xcode-select --install

# Установите PostgreSQL (для базы данных)
brew install postgresql@15

# Установите Redis (для кэширования)
brew install redis

# Установите Tesseract (для распознавания текста)
brew install tesseract

# Установите OpenCV зависимости
brew install opencv

# Проверьте установки
tesseract --version
```

---

## 📥 Установка Daur-AI

### Шаг 1: Загрузить Исходный Код

**Способ 1: Через Git (рекомендуется)**
```bash
# Откройте Terminal

# Перейдите в папку где хотите установить
cd ~/Documents

# Клонируйте репозиторий
git clone https://github.com/daurfinance/Daur-AI-v1.git

# Перейдите в папку проекта
cd Daur-AI-v1

# Проверьте содержимое
ls -la
```

**Способ 2: Загрузить ZIP файл**
1. Перейдите на https://github.com/daurfinance/Daur-AI-v1
2. Нажмите зелёную кнопку "Code"
3. Выберите "Download ZIP"
4. Откройте загруженный файл
5. Переместите папку в Documents или другое место

---

### Шаг 2: Создать Виртуальное Окружение Python

Виртуальное окружение изолирует зависимости проекта.

```bash
# Убедитесь, что вы в папке проекта
cd ~/Documents/Daur-AI-v1

# Создайте виртуальное окружение
python3.11 -m venv venv

# Активируйте виртуальное окружение
source venv/bin/activate

# Проверьте, что активировано (должна появиться (venv) в начале строки)
# Пример: (venv) user@MacBook Daur-AI-v1 %
```

---

### Шаг 3: Обновить pip и setuptools

```bash
# Убедитесь, что виртуальное окружение активировано
# (должна быть (venv) в начале строки)

# Обновите pip
pip install --upgrade pip

# Обновите setuptools
pip install --upgrade setuptools

# Проверьте версии
pip --version
```

---

### Шаг 4: Установить Python Зависимости

```bash
# Убедитесь, что виртуальное окружение активировано

# Установите зависимости из requirements.txt
pip install -r requirements.txt

# Это займёт 5-10 минут в зависимости от скорости интернета
# Вы увидите много строк с "Successfully installed"
```

**Если возникают ошибки:**
```bash
# Попробуйте установить зависимости по отдельности
pip install numpy
pip install flask
pip install psutil
pip install opencv-python
# и так далее...
```

---

## 🚀 Запуск Приложения

### Вариант 1: Запуск REST API Сервера (Рекомендуется)

```bash
# Убедитесь, что виртуальное окружение активировано
# (должна быть (venv) в начале строки)

# Запустите API сервер
python3 src/web/real_api_server.py

# Вы должны увидеть:
# * Running on http://127.0.0.1:5000
# * Press CTRL+C to quit

# Сервер теперь запущен и слушает на порту 5000
```

---

### Вариант 2: Запуск с Docker (Более Простой)

```bash
# Убедитесь, что Docker Desktop запущен
# (Cmd + Space, введите "Docker", откройте приложение)

# Перейдите в папку проекта
cd ~/Documents/Daur-AI-v1

# Соберите Docker образ
docker build -t daur-ai:latest .

# Это займёт 5-15 минут в зависимости от скорости интернета

# Запустите контейнер
docker run -p 5000:5000 daur-ai:latest

# Вы должны увидеть:
# * Running on http://0.0.0.0:5000
```

---

### Вариант 3: Запуск Полного Стека (Docker Compose)

```bash
# Убедитесь, что Docker Desktop запущен

# Перейдите в папку проекта
cd ~/Documents/Daur-AI-v1

# Запустите полный стек
docker-compose up -d

# Проверьте, что все сервисы запущены
docker-compose ps

# Вы должны увидеть список запущенных сервисов:
# NAME                COMMAND                  SERVICE             STATUS
# daur-ai-api         "python3 src/web/..."    api                 Up 2 seconds
# daur-ai-postgres    "docker-entrypoint..."   postgres            Up 3 seconds
# daur-ai-redis       "redis-server..."        redis               Up 2 seconds
```

---

## ✅ Проверка Работы

### Проверка 1: API Здоров

```bash
# Откройте новый Terminal (Cmd + T в Terminal или новое окно)

# Проверьте, что API отвечает
curl http://localhost:5000/api/v2/health

# Вы должны увидеть JSON ответ:
# {"status":"healthy","timestamp":"2025-10-25T..."}
```

---

### Проверка 2: Доступ через Браузер

```bash
# Откройте Safari или Chrome

# Перейдите на:
http://localhost:5000/api/v2/health

# Вы должны увидеть JSON ответ в браузере
```

---

### Проверка 3: Тестирование API Endpoints

```bash
# Откройте Terminal

# Получите статус системы
curl http://localhost:5000/api/v2/status

# Получите информацию об оборудовании
curl http://localhost:5000/api/v2/hardware/cpu

# Создайте пользователя
curl -X POST http://localhost:5000/api/v2/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "email": "test@example.com",
    "password": "TestPassword123!"
  }'
```

---

### Проверка 4: Доступ к Сервисам (Docker Compose)

Если вы запустили полный стек:

```bash
# Grafana (графики и дашборды)
# Откройте браузер: http://localhost:3000
# Логин: admin
# Пароль: admin

# Prometheus (метрики)
# http://localhost:9090

# PostgreSQL (база данных)
# Хост: localhost
# Порт: 5432
# Пользователь: daur
# Пароль: daur_secure_password_123

# Redis (кэширование)
# Хост: localhost
# Порт: 6379
```

---

## 🛑 Остановка Приложения

### Если запущен API сервер напрямую

```bash
# В Terminal где запущен сервер нажмите:
Ctrl + C

# Сервер остановится
```

---

### Если запущен Docker контейнер

```bash
# Откройте новый Terminal

# Остановите контейнер
docker stop daur-ai

# Или если используете docker-compose
docker-compose down
```

---

## 🔄 Перезапуск Приложения

### Способ 1: Простой перезапуск

```bash
# Остановите приложение (Ctrl + C)

# Запустите снова
python3 src/web/real_api_server.py
```

---

### Способ 2: Перезапуск Docker контейнера

```bash
# Остановите контейнер
docker stop daur-ai

# Запустите снова
docker run -p 5000:5000 daur-ai:latest
```

---

## 🐛 Решение Проблем

### Проблема 1: "Command not found: python3.11"

**Решение:**
```bash
# Переустановите Python
brew install python@3.11

# Или используйте python3 вместо python3.11
python3 --version

# Если версия ниже 3.11, обновите
brew upgrade python@3.11
```

---

### Проблема 2: "Port 5000 already in use"

**Решение:**
```bash
# Найдите процесс использующий порт 5000
lsof -i :5000

# Убейте процесс (замените PID на число из вывода выше)
kill -9 <PID>

# Или используйте другой порт
python3 src/web/real_api_server.py --port 5001
```

---

### Проблема 3: "ModuleNotFoundError: No module named 'flask'"

**Решение:**
```bash
# Убедитесь, что виртуальное окружение активировано
# (должна быть (venv) в начале строки)

# Если нет, активируйте
source venv/bin/activate

# Переустановите зависимости
pip install -r requirements.txt
```

---

### Проблема 4: "Docker command not found"

**Решение:**
```bash
# Убедитесь, что Docker Desktop запущен
# Cmd + Space, введите "Docker", откройте приложение

# Закройте Terminal полностью
# Откройте новый Terminal

# Проверьте
docker --version
```

---

### Проблема 5: "Permission denied" при установке Homebrew

**Решение:**
```bash
# Используйте sudo
sudo /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Введите пароль MacBook
```

---

### Проблема 6: "curl: command not found"

**Решение:**
```bash
# Установите curl через Homebrew
brew install curl

# Или используйте встроенный curl
/usr/bin/curl http://localhost:5000/api/v2/health
```

---

## 📊 Проверка Ресурсов

### Проверка использования памяти

```bash
# Откройте Activity Monitor
# Cmd + Space, введите "Activity Monitor"

# Или через Terminal
top -l 1 | head -20
```

---

### Проверка использования диска

```bash
# Откройте Terminal

# Проверьте свободное место
df -h

# Проверьте размер папки проекта
du -sh ~/Documents/Daur-AI-v1
```

---

## 🎯 Рекомендуемый Процесс Установки

**Для новичков (самый простой способ):**

1. Установите Homebrew
2. Установите Docker Desktop
3. Загрузите исходный код
4. Запустите `docker run -p 5000:5000 daur-ai:latest`
5. Откройте http://localhost:5000 в браузере

**Для разработчиков (с Python):**

1. Установите Python 3.11
2. Загрузите исходный код
3. Создайте виртуальное окружение
4. Установите зависимости
5. Запустите `python3 src/web/real_api_server.py`
6. Откройте http://localhost:5000 в браузере

**Для production (полный стек):**

1. Установите Docker Desktop
2. Загрузите исходный код
3. Запустите `docker-compose up -d`
4. Доступ к сервисам через браузер

---

## 📚 Полезные Команды

```bash
# Активировать виртуальное окружение
source venv/bin/activate

# Деактивировать виртуальное окружение
deactivate

# Обновить зависимости
pip install --upgrade -r requirements.txt

# Список установленных пакетов
pip list

# Проверить версию Python
python3 --version

# Проверить версию Docker
docker --version

# Просмотр логов Docker
docker logs -f daur-ai

# Вход в контейнер
docker exec -it daur-ai bash

# Остановить все контейнеры
docker stop $(docker ps -q)

# Удалить все контейнеры
docker rm $(docker ps -aq)
```

---

## 🎉 Готово!

Ваш Daur-AI v2.0 теперь установлен и запущен на MacBook!

**Что дальше?**

1. Изучите API документацию
2. Создайте пользователя
3. Используйте модули (input control, vision, hardware monitoring, etc.)
4. Интегрируйте в свой проект

**Нужна помощь?**

- Проверьте раздел "Решение проблем" выше
- Посмотрите логи: `docker logs -f daur-ai`
- Проверьте здоровье API: `curl http://localhost:5000/api/v2/health`

---

*Последнее обновление: 25 октября 2025*
*Daur-AI v2.0 - Production Ready*

