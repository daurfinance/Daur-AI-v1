# Daur-AI v2.0 - Руководство по Тестированию на MacBook

## 🧪 Полное Руководство Тестирования

Убедитесь, что контейнер запущен:
```bash
docker run -p 8000:8000 daur-ai:latest
```

---

## ✅ Тест 1: Проверка Здоровья API

**Команда:**
```bash
curl http://localhost:8000/health
```

**Ожидаемый результат:**
```json
{"status":"healthy","timestamp":"2025-10-25T18:37:51.719538","version":"1.1"}
```

**Что это проверяет:**
- ✅ API работает
- ✅ Сервер отвечает
- ✅ Версия приложения

---

## ✅ Тест 2: Получить Информацию о Системе

**Команда:**
```bash
curl "http://localhost:8000/system/info"
```

**Ожидаемый результат:**
```json
{
  "os": "Darwin",
  "platform": "macOS",
  "python_version": "3.11.x",
  "hostname": "MacBook-Air-DAX"
}
```

**Что это проверяет:**
- ✅ Модуль системной информации
- ✅ Определение ОС
- ✅ Версия Python

---

## ✅ Тест 3: Мониторинг Оборудования

**Команда для CPU:**
```bash
curl "http://localhost:8000/hardware/cpu"
```

**Ожидаемый результат:**
```json
{
  "percent": 45.5,
  "count": 8,
  "freq": 2400.0
}
```

**Команда для памяти:**
```bash
curl "http://localhost:8000/hardware/memory"
```

**Ожидаемый результат:**
```json
{
  "total": 16000000000,
  "available": 8000000000,
  "percent": 50.0
}
```

**Команда для диска:**
```bash
curl "http://localhost:8000/hardware/disk"
```

**Ожидаемый результат:**
```json
{
  "total": 500000000000,
  "free": 250000000000,
  "percent": 50.0
}
```

**Что это проверяет:**
- ✅ Мониторинг CPU
- ✅ Мониторинг памяти
- ✅ Мониторинг диска
- ✅ Реальные метрики системы

---

## ✅ Тест 4: Управление Пользователями

**Регистрация пользователя:**
```bash
curl -X POST "http://localhost:8000/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "email": "test@example.com",
    "password": "TestPassword123!"
  }'
```

**Ожидаемый результат:**
```json
{
  "message": "User registered successfully",
  "user_id": 1,
  "username": "testuser"
}
```

**Что это проверяет:**
- ✅ Регистрация пользователя
- ✅ Валидация пароля
- ✅ Сохранение в БД

---

## ✅ Тест 5: Аутентификация

**Логин:**
```bash
curl -X POST "http://localhost:8000/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "TestPassword123!"
  }'
```

**Ожидаемый результат:**
```json
{
  "message": "Login successful",
  "access_token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "token_type": "Bearer"
}
```

**Что это проверяет:**
- ✅ Аутентификация пользователя
- ✅ Генерация JWT токена
- ✅ Проверка пароля

---

## ✅ Тест 6: Использование Токена

**Команда (замените TOKEN на полученный токен):**
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  "http://localhost:8000/user/profile"
```

**Ожидаемый результат:**
```json
{
  "user_id": 1,
  "username": "testuser",
  "email": "test@example.com"
}
```

**Что это проверяет:**
- ✅ Аутентификация по токену
- ✅ Защита эндпоинтов
- ✅ Получение профиля пользователя

---

## ✅ Тест 7: Логирование

**Команда:**
```bash
curl "http://localhost:8000/logs/recent"
```

**Ожидаемый результат:**
```json
{
  "logs": [
    {
      "timestamp": "2025-10-25T18:37:51",
      "level": "INFO",
      "message": "User registered"
    }
  ]
}
```

**Что это проверяет:**
- ✅ Логирование действий
- ✅ Сохранение истории
- ✅ Получение логов

---

## ✅ Тест 8: Обработка Ошибок

**Попытка логина с неправильным паролем:**
```bash
curl -X POST "http://localhost:8000/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "WrongPassword"
  }'
```

**Ожидаемый результат:**
```json
{
  "error": "Invalid credentials",
  "message": "Username or password is incorrect"
}
```

**Что это проверяет:**
- ✅ Обработка ошибок
- ✅ Валидация входных данных
- ✅ Безопасность

---

## 🧪 Тест 9: Полный Цикл (Скрипт)

Создайте файл `test_daur_ai.sh`:

```bash
#!/bin/bash

echo "🧪 Тестирование Daur-AI v2.0"
echo "================================"

# Тест 1: Здоровье
echo "✅ Тест 1: Проверка здоровья"
curl -s http://localhost:8000/health | jq .
echo ""

# Тест 2: Информация о системе
echo "✅ Тест 2: Информация о системе"
curl -s "http://localhost:8000/system/info" | jq .
echo ""

# Тест 3: CPU
echo "✅ Тест 3: Информация о CPU"
curl -s "http://localhost:8000/hardware/cpu" | jq .
echo ""

# Тест 4: Память
echo "✅ Тест 4: Информация о памяти"
curl -s "http://localhost:8000/hardware/memory" | jq .
echo ""

# Тест 5: Диск
echo "✅ Тест 5: Информация о диске"
curl -s "http://localhost:8000/hardware/disk" | jq .
echo ""

echo "================================"
echo "✅ Все тесты завершены!"
```

**Запуск:**
```bash
chmod +x test_daur_ai.sh
./test_daur_ai.sh
```

---

## 🧪 Тест 10: Тестирование через Postman

**Установка Postman:**
1. Перейдите на https://www.postman.com/downloads/
2. Загрузите Postman для Mac
3. Установите приложение

**Создание запроса:**
1. Откройте Postman
2. Нажмите "New" → "Request"
3. Выберите метод: GET
4. Введите URL: `http://localhost:8000/health`
5. Нажмите "Send"
6. Должны увидеть JSON ответ

---

## 📊 Тест 11: Нагрузочное Тестирование

**Установка Apache Bench:**
```bash
# Уже установлен на MacBook
ab -V
```

**Запуск теста (100 запросов, 10 одновременно):**
```bash
ab -n 100 -c 10 http://localhost:8000/health
```

**Ожидаемый результат:**
```
Requests per second:    1000.00 [#/sec] (mean)
Time per request:       1.000 [ms] (mean)
```

**Что это проверяет:**
- ✅ Производительность
- ✅ Стабильность под нагрузкой
- ✅ Время отклика

---

## 📊 Тест 12: Мониторинг Ресурсов

**Во время теста откройте Activity Monitor:**
1. Cmd + Space
2. Введите "Activity Monitor"
3. Откройте приложение
4. Найдите "docker" или "python"
5. Смотрите CPU и Memory

**Что проверяем:**
- ✅ Использование CPU
- ✅ Использование памяти
- ✅ Количество потоков

---

## 🔧 Тест 13: Проверка Логов

**В Terminal где запущен Docker:**
- Вы видите логи в реальном времени
- Каждый запрос логируется
- Ошибки выделены красным

**Пример лога:**
```
2025-10-25 18:37:51,322 - daur_ai.web_api - INFO - User registered
2025-10-25 18:37:52,456 - werkzeug - INFO - GET /health HTTP/1.1
```

---

## ✅ Чек-Лист Тестирования

- [ ] API здоров (test 1)
- [ ] Информация о системе (test 2)
- [ ] CPU мониторинг (test 3)
- [ ] Память мониторинг (test 4)
- [ ] Диск мониторинг (test 5)
- [ ] Регистрация пользователя (test 6)
- [ ] Логин пользователя (test 7)
- [ ] Использование токена (test 8)
- [ ] Обработка ошибок (test 9)
- [ ] Полный цикл (test 10)
- [ ] Postman тестирование (test 11)
- [ ] Нагрузочное тестирование (test 12)
- [ ] Мониторинг ресурсов (test 13)

---

## 🎯 Результаты Тестирования

Если все тесты прошли успешно:

✅ **API работает корректно**  
✅ **Мониторинг работает**  
✅ **Аутентификация работает**  
✅ **Логирование работает**  
✅ **Производительность хорошая**  

**Daur-AI v2.0 готова к использованию! 🚀**

---

## 🆘 Решение Проблем

### Проблема: "Connection refused"
```bash
# Проверьте, что контейнер запущен
docker ps

# Проверьте логи
docker logs <container_id>
```

### Проблема: "404 Not Found"
```bash
# Проверьте правильность URL
# Используйте /health для проверки
curl http://localhost:8000/health
```

### Проблема: Медленные ответы
```bash
# Проверьте ресурсы
docker stats

# Перезапустите контейнер
docker restart <container_id>
```

---

## 📚 Дополнительные Ресурсы

- **Документация API:** `DAUR_AI_V2_PRODUCTION_GUIDE.md`
- **Примеры использования:** `REAL_CAPABILITIES_DEMO.py`
- **Архитектура:** `IMPLEMENTATION_SUMMARY.md`

---

*Последнее обновление: 25 октября 2025*
*Daur-AI v2.0 - Production Ready*

